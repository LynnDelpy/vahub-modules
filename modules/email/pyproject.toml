[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "vahub-mod-email"
version = "0.1.0"
description = "vahub email module (read-only IMAP, MCP server over stdio)"
readme = "README.md"
requires-python = ">=3.12"
license = "MIT"
# IMAP and message parsing are in the standard library, so the only dependency
# is the MCP server framework itself.
dependencies = [
  "mcp>=1.4,<2",
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "ruff>=0.5"]

[tool.hatch.build.targets.wheel]
packages = ["src/vahub_mod_email"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 110
target-version = "py312"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM", "RUF"]
